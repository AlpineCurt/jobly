const { BadRequestError } = require("../expressError");

/** Converys minEmployees and maxEmployees into integers.
 *  Returns object. 
 */
function parseQuery(reqquery) {
    let query = {};
    //reqquery.name ? query.name = 

}

module.exports = { parseQuery };